set -g mode-mouse on
set -g mouse-select-pane on
set -g mouse-resize-pane on

set-option -g status-bg '#010C0F'
set-option -g status-fg '#aaaaaa'
set-option -g status-right " #(uptime | awk '{print $3}'|sed 's/,//') / #(batteryLevel) / #(date '+%a, %b %d, %H:%M') "
