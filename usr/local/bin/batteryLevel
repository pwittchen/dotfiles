#!/usr/bin/env bash

if [ `uname` = "Linux" ]; then
  BATTERY_LEVEL_LINUX=$(cat /sys/class/power_supply/BAT0/capacity)
  echo $BATTERY_LEVEL_LINUX'%'
fi

if [ `uname` = "Darwin" ]; then
  pmset -g batt | awk '{print $2}' | sed 's/;//' | tail -n+2
fi
